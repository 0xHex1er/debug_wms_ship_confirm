# Project Rules & Constraints

## 1. Read-Only Directories
- โฟลเดอร์ `source_code/` และไฟล์ย่อยทั้งหมดในโฟลเดอร์นี้ถูกกำหนดให้เป็น **Read-Only (อ่านอย่างเดียว)**
- ห้ามทำการแก้ไข, ลบ, หรือสร้างไฟล์ใหม่ในโฟลเดอร์ `source_code/` เด็ดขาด

## 2. Prohibition of SendEmail Function
- **ห้ามเรียกใช้งานฟังก์ชัน `SendEmail` ในโปรเจกต์นี้เด็ดขาด** (ทั้งในส่วนของสคริปต์, API, หรือส่วนโค้ดใหม่ใดๆ)
- หากระบบพบข้อผิดพลาดหรือสิ้นสุดการทำงาน ให้ใช้ช่องทางการบันทึก Log หรือแสดงข้อมูลผ่านหน้า UI/API แทนการส่งอีเมล

<!-- ## 3. Prohibition of Browser Agent Testing
- **ห้ามเปิดใช้งาน Browser Agent (`browser_subagent`) หรือเข้าเบราว์เซอร์เพื่อทำการทดสอบระบบในโปรเจกต์นี้เด็ดขาด**
- ผู้ใช้งานจะเป็นผู้ทำการทดสอบและเปิดดูหน้าเว็บ/ระบบด้วยตนเอง เพื่อไม่ให้สิ้นเปลือง Token -->
